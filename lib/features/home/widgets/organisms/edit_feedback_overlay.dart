import 'package:flutter/material.dart';

class EditFeedbackOverlay extends StatelessWidget {
  const EditFeedbackOverlay({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Overlay();
  }
}
